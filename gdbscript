define pnodes
    printf "Nodes: "
    set var $n = $arg0->head
    set var $i = 0
    while $n
    	  if $i == 0
	     printf "%d", $n->id
	  else
	     printf " -> %d", $n->id
	   end
	set var $n = $n->next
	set var $i = $i + 1
    end
    printf "\n"
end
       